// String, number, undefined, null, boolean, symbol

const nome = "André"; // string
const nome1 = 'André'; // string
const nome2 = `André`; // string
const numero = 10; // number
const numero1 = 10.20; // number

let numeAluno; // undefined -> não aponta para local nenhum na memória.
const sobrenomeAluno = null; // Null -> não aponta para local nenhum na memória.
const aprovado = false; // Boolean: true, false (lógico)